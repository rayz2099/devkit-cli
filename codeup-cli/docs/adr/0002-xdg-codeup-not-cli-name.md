# Config lives at ~/.config/codeup-cli

Supersedes the earlier `~/.config/codeup` lock. The binary is `codeup-cli`; XDG follows the module name like `jenkins-cli`.

Existing `~/.config/codeup/config.json` is not read.
