{
  "name": "olly-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "olly-cli": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --outfile dist/olly-cli",
    "test": "bun test",
    "check": "bun test && tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  },
  "license": "GPL-2.0-only"
}
