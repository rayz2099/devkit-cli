{
  "name": "devkit-cli",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "license": "GPL-2.0-only",
  "scripts": {
    "devkit-cli": "bun run src/main.ts",
    "test": "bun test src/*.test.ts code-ws/src/*.test.ts",
    "check": "bunx tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
