# plan-03-milestone-03

## 目标

实现 `tl --version` 版本输出能力，并建立统一版本号入口，后续 fix 只需升级补丁版本。

## 约束

- 版本输出必须跳过配置加载和 app factory
- 帮助文案与 shell completion 需要同步
- 版本号必须集中维护，避免多处散落
- 单文件不超过 `500` 行

## 里程碑拆解

1. 为 `--version` 入口补失败测试
2. 新增统一版本号定义
3. 在根命令分发中接入版本输出
4. 更新帮助文案与 fish completion
5. 完成回归测试与安装验证
