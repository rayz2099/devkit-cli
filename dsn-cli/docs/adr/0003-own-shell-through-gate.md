# Own Shell, every line through the Gate

Superseded by 0010.

Human shortest path was going to be our REPL so every line hit the Gate. That blocked `mysql` / `redis-cli` / `mongosh`. Humans actually want the vendor Console; agents never enter it. Gate belongs on `query` only.
