# Query argv tokens join into one statement

`query` accepts one argv or several tokens joined by a single space. Fish can then complete a Topic as its own word (`query peek <TAB>`).

A `--topic` flag was rejected as a second dialect next to native statements. Quoted one-argument form stays valid. The join is for every Kind so the parser has one rule.
